pipeline { 
    agent any    
    stages { 
        stage('Ejecucion de pruebas') {  
            steps { 
                bat "npm install"
                bat "newman run prueba.json"
            } 
        } 
    } 
}
